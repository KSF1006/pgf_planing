<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid d-flex justify-content-between">
  
    <div class="d-flex align-items-center">
      <a class="navbar-brand me-3" href="#"><%= Rails.configuration.application_name %></a>
      <ul class="navbar-nav d-flex flex-row">
        <li class="nav-item me-3">
          <a class="nav-link active" href="#">Home</a>
        </li>
        <li class="nav-item me-3">
          <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
      </ul>
    </div>    
    
    <div class="d-flex">
      <% if user_signed_in? %>
        <span class="navbar-text me-2">Signed in as <%= current_user.email %></span>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, form: { "data-turbo" => "false", class: "d-inline" }, class: "btn btn-link navbar-text p-0" %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path, class: "nav-link p-0 me-2" %>
        <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link p-0" %>
      <% end %>
    </div>   

  </div>
</nav>
